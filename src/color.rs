#[derive(Eq, PartialEq, Debug, Clone, Hash, PartialOrd, Ord)]
pub enum Color {
    Red,
    Green,
    DGreen,
    Blue,
    DBlue,
    Pink,
    Yellow,
    Grey,
    Brown,
    Orange,
    LOrange,
}
